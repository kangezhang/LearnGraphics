{
  "name": "learn-graphics",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "check:docs-sync": "node scripts/check-doc-sync.mjs --staged",
    "setup:hooks": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@types/three": "^0.182.0",
    "typescript": "~5.9.3",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "ajv": "^8.18.0",
    "three": "^0.171.0",
    "yaml": "^2.8.2"
  }
}
